<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="castro.jhostin.thecheezery2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Combos"></activity>
        <activity android:name=".Personalized" />
        <activity android:name=".RegistroActivity" />
        <activity android:name=".Sweeties" />
        <activity android:name=".Salties" />
        <activity android:name=".HotDrinks" />
        <activity android:name=".ColdDrinks" />
        <activity android:name=".Product" />
        <activity android:name=".ProductosActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>